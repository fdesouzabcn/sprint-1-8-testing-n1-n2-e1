# Practicing Testing
  **Description**: In this practice (S1.08) we will have to practice some Testing (TDD , unit tests and data providers).

  ## 🛠 Technologies
  - Frontend: PHP
  - Backend: PHP

  ## 🚀 Installation
> [!NOTE]
> For this exercise we are installing PHP Unit 11.5 (PHAR) manually.
> <br>You find it under the "./tools/phpunit" path.
  1. Clone the repository: git clone https://github.com/fdesouzabcn/sprint-1-8-testing-n1-n2-e1.git
  2. Install dependencies:
      - Install [XAMPP](https://www.apachefriends.org/) or [PHP](https://www.php.net/downloads).
      - Install [PHP Unit 11.5 (PHAR) manually](https://docs.phpunit.de/en/11.5/installation.html#manual-download-of-phar) ensuring sure `phpunit` is available in `tools/phpunit/` or install it via Composer
      - (Optional) Place `php.exe` inside `tools/php/` for local use.
  3. Updaye "setting.json" accordingly: 
  4. Environment Variables: N/A (`example: Create ``.env`` with...`)

  ## 📸 Demo
 N/A (`example:  [Link to Vercel/Netlify] or screenshots.`)
